{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "internal-node-app.serviceAccountName" . }}
  labels:
{{ include "internal-node-app.labels" . | nindent 4 }}
{{- end -}}
