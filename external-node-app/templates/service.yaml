apiVersion: v1
kind: Service
metadata:
  name: {{ include "external-node-app.fullname" . }}
  labels:
    {{- include "external-node-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "external-node-app.selectorLabels" . | nindent 4 }}
